#include <stdio.h>

int main()
{
    int a;
    scanf("%d", &a);
    if (a = 0xA | a > 12)
        if (011 & 10 == a)
            printf("% d !\n", a);
        else
            printf("Right ! % d\n", a);
    else
        printf("Wrong ! % d\n", a);
}